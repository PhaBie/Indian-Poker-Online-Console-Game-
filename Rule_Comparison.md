# เปรียบเทียบกติกา Teen Patti (มาตรฐาน) vs โค้ดของเราในปัจจุบัน

เอกสารนี้จัดทำขึ้นเพื่อใช้เปรียบเทียบกติกาเกม Teen Patti ที่เป็นมาตรฐานสากล (อ้างอิงจากกฎในแอปเกม) กับโค้ดในโปรเจกต์ของเรา เพื่อให้ทีมตัดสินใจว่าจะเริ่มเขียนระบบไหนก่อน

---

## 1. ลำดับความใหญ่ของไพ่เรียง (A-K-Q vs A-2-3)

**ความแตกต่างสำคัญที่สุดที่เจอในส่วนของการเปรียบเทียบไพ่**

- **กติกามาตรฐาน:** ไพ่เรียง (Straight) และ เรียงสี (Straight Flush) ที่ใหญ่ที่สุดคือ **A-K-Q** รองลงมาเป็นอันดับสองคือ **A-2-3** และเล็กสุดคือ 4-3-2
- **โค้ดปัจจุบันของเรา (`gameLogic.ts`):** เราเขียนลอจิกให้ **A-2-3 เป็นไพ่ที่ใหญ่ที่สุดเสมอ** (ค่า 15) ชนะ A-K-Q (ค่า 14)
- **💡 สิ่งที่ต้องทำ:** ต้องแก้เงื่อนไขใน `gameLogic.ts` ให้ A-K-Q กลับมาใหญ่สุด

## 2. ระบบคนตาบอด vs คนตาดี (Blind vs Seen)

- **กติกามาตรฐาน:** ผู้เล่นมีสิทธิ์เลือกเล่นแบบ **Blind (ไม่ดูไพ่)** หรือ **Seen (ขอดูไพ่แล้วค่อยเล่น)** ซึ่งจำนวนเงินที่ใช้สู้จะต่างกัน (คนที่ดูไพ่แล้วต้องจ่ายเยอะกว่า)
- **โค้ดปัจจุบันของเรา:** เรามีการเตรียม property `isBlind` ไว้ใน `Player.ts` และ `types.ts` แล้ว
- **💡 สิ่งที่ต้องทำ:** นำ property `isBlind` ที่สร้างไว้ ไปใช้งานจริงในลอจิกการคำนวณจำนวนชิปที่ต้องเดิมพัน

## 3. ระบบขอประลองไพ่ตัวต่อตัว (Sideshow)

- **กติกามาตรฐาน:** ถ้าผู้เล่น 2 คนที่เล่นต่อกันเป็น Seen ทั้งคู่ สามารถขอทำ **"Sideshow"** เพื่อขอวัดไพ่กันแบบลับๆ คนที่แต้มต่ำกว่าจะต้องหมอบทันที (ถ้าแต้มเท่ากัน คนที่ขอท้าต้องหมอบ)
- **โค้ดปัจจุบันของเรา:** มีการสร้างโครงของฟังก์ชันไว้แล้วใน `GameState.ts` (`executeSideshow`, `rejectSideshow`) รวมถึงใน `types.ts` (`SIDESHOW`)
- **💡 สิ่งที่ต้องทำ:** ปัจจุบันฟังก์ชันใน `GameState.ts` เหล่านี้ยังว่างเปล่า (ใส่คอมเมนต์ `// รอคนเลือก` ไว้) ต้องเขียนลอจิกการหักเงิน ตรวจสอบสิทธิ์ และลอจิกจับคนแพ้หมอบ (Fold) ใส่เข้าไป

## 4. เงื่อนไขการเปิดไพ่จบเกม (Show)

- **กติกามาตรฐาน:** การเปิดไพ่ (Show) จะเกิดขึ้นได้ก็ต่อเมื่อมีผู้เล่นบนโต๊ะเหลือแค่ 2 คนเท่านั้น
- **โค้ดปัจจุบันของเรา:** เราเตรียมโครงฟังก์ชัน `requestShow` และ `canForceShow` ไว้ใน `GameState.ts` แล้ว แต่ยังไม่ได้เขียนลอจิกข้างใน
- **💡 สิ่งที่ต้องทำ:** เขียนโค้ดลงใน `canForceShow()` ให้รีเทิร์น `true` เฉพาะตอนที่ `activePlayers.length === 2` และปรับลอจิกหลักให้เรียกใช้การเปรียบเทียบไพ่เมื่อเหลือ 2 คนเท่านั้น

## 5. เงินค่าผ่านประตู / ค่าเข้าเล่น (Boot / Ante)

- **กติกามาตรฐาน:** ก่อนแจกไพ่ ผู้เล่นทุกคนจะถูกหักชิป (Boot Amount) เข้าสู่กองกลาง (Pot) ทันที
- **โค้ดปัจจุบันของเรา:** เรามีการประกาศ `bootAmount` ไว้ในคลาส `GameState` และ `Room` เรียบร้อยแล้ว (ตั้งค่าเริ่มต้นที่ 50)
- **💡 สิ่งที่ต้องทำ:** ขาดแค่จังหวะตอนเริ่มเกมที่เราต้องเอา `bootAmount` ไปไล่ลบออกจาก `chips` ของทุกคน และบวกเข้าตัวแปร `pot`

---

**บทสรุปสำหรับการประชุมทีม:**
โค้ดในตอนนี้ **เตรียมโครงสร้างและตัวแปรทั้งหมดไว้พร้อมแล้ว (ในฝั่ง Model / State)** ทั้งเรื่อง `isBlind`, `bootAmount` และฟังก์ชันอย่าง `Sideshow` แต่ฟังก์ชันเหล่านั้นยังไม่ได้ถูกเขียนลอจิกการทำงานข้างในครับ

มีเพียงแค่จุดเดียวที่เป็นลอจิกทำงานอยู่แล้ว แต่เขียนมาผิดจากกติกา ก็คือ **ข้อ 1 (A-K-Q vs A-2-3)** ซึ่งสามารถเข้าไปแก้ใน `gameLogic.ts` ได้เลยครับ ส่วนข้อ 2-5 แค่เติมลอจิกใส่ในฟังก์ชันที่ทำโครงรอไว้แล้วก็เสร็จครับ!
