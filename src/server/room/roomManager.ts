import { ServerPlayer } from '../../shared/types';

export function createRoom(host: ServerPlayer): string {
    // รอคนเลือก
    return '';
}

export function joinRoom(roomId: string, player: ServerPlayer): boolean {
    // รอคนเลือก
    return false;
}

export function leaveRoom(roomId: string, playerId: string): void {
    // รอคนเลือก
}

export function startGame(roomId: string): void {
    // รอคนเลือก
}
